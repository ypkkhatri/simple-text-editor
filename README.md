# Simple Text Editor for Web

A simple web based application for text editing for quick use. 

## Query Parameters:

- `name` param is use to save the content against that name. Default is empty string.
- `theme` could be `dark` or `light`. Default is `light`.

```
/?theme=dark&name=coronavirus
```
